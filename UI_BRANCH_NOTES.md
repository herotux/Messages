# Figma Material 3 UI experiment

Temporary marker for the Figma UI implementation branch. The branch is intentionally isolated from `main` so the current app remains untouched while the new UI is evaluated.
